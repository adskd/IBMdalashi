
name: aIBM Cloud Foundry - aV2Ray

env:
  bIBM_CF_API: https://api.us-south.cf.cloud.ibm.com
  bIBM_CF_APP_MEM: 256M

on:
  workflow_dispatch:
  repository_dispatch:
 # schedule:
 #   - cron: 10 17 * * 3

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    env:
      bIBM_CF_USERNAME: ${{ secrets.bIBM_CF_USERNAME }}
      bIBM_CF_PASSWORD: ${{ secrets.bIBM_CF_PASSWORD }}
      bIBM_CF_ORG_NAME: ${{ secrets.bIBM_CF_ORG_NAME }}
      bIBM_CF_SPACE_NAME: ${{ secrets.bIBM_CF_SPACE_NAME }}
      bIBM_CF_APP_NAME: ${{ secrets.bIBM_CF_APP_NAME }}
      AV2_UUID: ${{ secrets.AV2_UUID }}
    #  AV2_WS_PATHS_SVMESS: ${{ secrets.AV2_WS_PATHS_SVMESS }}
    #  AV2_WSA_PATHS_AVLESS: ${{ secrets.AV2_WSA_PATHS_AVLESS }}

    steps:

    - name: Install Cloud Foundry CLI
      run: |
        curl -fsSL "https://packages.cloudfoundry.org/stable?release=linux64-binary&version=v7&source=github" | tar -zxC /tmp
        sudo install -m 755 /tmp/cf7 /usr/local/bin/cf
        cf version
        
    - name: Login to IBM Cloud Foundry
      run: |
        cf login \
          -a "${bIBM_CF_API}" \
          -u "${bIBM_CF_USERNAME}" \
          -p "${bIBM_CF_PASSWORD}" \
          -o "${bIBM_CF_ORG_NAME:-$bIBM_CF_USERNAME}" \
          -s "${bIBM_CF_SPACE_NAME:-dev}"
          
    - name: Download Latest Xray
      run: |
        DOWNLOAD_URL="https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip"
        curl -fsSL "$DOWNLOAD_URL" -o "latest-xray.zip"
        unzip latest-xray.zip xray geoip.dat geosite.dat
        rm latest-xray.zip
        chmod -v 755 xr*
        ./xray -version
        mv xray ${bIBM_CF_APP_NAME}
        
    - name: Generate Xray Config File (VMess)
    #  if: ${{ env.AV2_WS_PATHS_SVMESS }}
      run: |
        base64 << 126 > config
        {
          "log": {
            "access": "none",
            "loglevel": "error"
          },
          "inbounds": [
            {
              "port": 8080,
              "protocol": "vmess",
              "settings": {
                "clients": [
                  {
                    "id": "${AV2_UUID}",
                    "alterId": 2
                  }
                ]
              },
              "streamSettings": {
                "network":"ws",
                "wsSettings": {
                  "path": "${AV2_WS_PATHS_SVMESS}"
                }
              }
            }
          ],
          "outbounds": [
            {
              "protocol": "freedom"
            }
          ]
        }
        
    - name: Generate Xray Config File (VLESS)
    #  if: ${{ env.AV2_WSA_PATHS_AVLESS }}
      run: |
        base64 << 126 > config
        {
          "log": {
            "access": "none",
            "loglevel": "error"
          },
          "inbounds": [
            {
              "port": 8080,
              "protocol": "vless",
              "settings": {
                "decryption": "none",
                "clients": [
                  {
                    "id": "${AV2_UUID}"
                  }
                ]
              },
              "streamSettings": {
                "network":"ws",
                "wsSettings": {
                  "path": "${AV2_WSA_PATHS_AVLESS}"
                }
              }
            }
          ],
          "outbounds": [
            {
              "protocol": "freedom"
            }
          ]
        }
        
    - name: Generate Manifest File
      run: |
        cat << 126 > manifest.yml
        applications:
        - name: ${bIBM_CF_APP_NAME}
          memory: ${bIBM_CF_APP_MEM}
          random-route: true
          command: base64 -d config > config.json; ./${bIBM_CF_APP_NAME} -config=config.json
          buildpacks:
          - binary_buildpack
        
    - name: Deploy Cloud Foundry App
      run: cf push
